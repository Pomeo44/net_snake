—етева€ змейка.Ќемного изменил логику определени€ победител€ т.к. решил, что она правильней.   серверу подключаютс€ игроки не больше определенного количества (в дальнейшем можно изменить реализацию и вынести это значение в параметр). ѕосле смерти всех змей, победителем становитьс€ зме€, съевша€ больше всех €блок.ѕакет —ервера состоит из класса сервера, который при запуске создает класс игрового пол€ FieldGame и ждет подключени€ клиента. ѕосле подключени€ клиента, он создает отдельный класс ClientWorker дл€ обмена данными с клиентом. ClientWorker в конструкторе создает змею, котора€ помещаетс€ на поле. ClientWorker получа€ коменды от клиента, передает их змее. »гровое поле за одну итерацию двигает змей, анализирует изменение их положени€ и формирует строку, описывающую графическое состо€ние пол€. ClientWorker в цикле ждет изменени€ графического пол€ и передает их клиенту.  лиент, ожида€ в цикле нажати€ клавишь, передает их ClientWorker и получает строку, описывающую графическое состо€ние пол€. √рафическое поле разбирает строку данных и рисует змей. ≈сли игра завершилась, то ClientWorker останавливаютс€ и игровое поле создаетс€ заново.“естирование проводил локально с 4 клиентами и с 2 клиентами в разных локальных сет€х.—ервер запускаетс€ с 1 параметром порта (по умолчанию 3000), клиент с параметром dns имени или ip адресом и портом (по умолчанию localhost 3000)